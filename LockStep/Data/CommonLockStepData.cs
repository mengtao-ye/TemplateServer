namespace YSF
{
    /// <summary>
    /// 通用帧同步数据
    /// </summary>
    public static class CommonLockStepData
    {
        public const float REFRESH_TIME = 0.05f;//一帧的时间
        public const float SERVER_REFRESH_TIME = 0.1f;//服务器一帧的时间
    }
}
